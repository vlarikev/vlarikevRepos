@model IEnumerable<CCNetStore.Models.client>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.clientLogin)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.clientPassword)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.clientRole)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.clientLogin)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.clientPassword)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.clientRole)
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.clientId }) |
        @Html.ActionLink("Details", "Details", new { id = item.clientId }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.clientId })
    </td>
</tr>
}

</table>
